name: Test (scratch)

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello

  notify:
    runs-on: ubuntu-latest
    needs: test
    if: ${{ success() || failure() }}
    steps:
      - run: echo "suppose I'm notifying slack"
